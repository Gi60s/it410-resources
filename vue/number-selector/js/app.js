(function() {
    const app = new Vue({
        el: "#app",
        data: {
            low: 0,
            high: 10,
            value: 0
        },
        methods: {
            // add methods: lowChanged, highChanged, valueChanged
            // to update data when events fire
        }
    });
})();